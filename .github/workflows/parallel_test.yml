# Run tests in parallel to check for bugs that only show up on multiple processes
name: Run tests in parallel

on: [push, pull_request]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        # This job currently fails on macOS - don't know why, try re-activating later
        #os: [ubuntu-latest, macOS-latest]
        os: [ubuntu-latest]
      fail-fast: false
    timeout-minutes: 30

    steps:
      - uses: actions/checkout@v2
      - uses: julia-actions/setup-julia@v1
        with:
          version: '1'
          arch: x64
      - uses: julia-actions/julia-buildpkg@v1
      - run: |
          julia --project -e 'using MPI; MPI.install_mpiexecjl()'
          julia -O3 --check-bounds=no precompile.jl --debug 1
          ~/.julia/bin/mpiexecjl -np 2 julia --project=@. -Jmoment_kinetics.so -O3 --check-bounds=no test/runtests.jl --debug 1
          ~/.julia/bin/mpiexecjl -np 4 julia --project=@. -Jmoment_kinetics.so -O3 --check-bounds=no test/runtests.jl --debug 1
        shell: bash
